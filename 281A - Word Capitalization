#include <bits/stdc++.h>
#include <iostream>

using namespace std;

int main()
{
    string s;
    cin >> s;
    int sz = s.size(), count_lowercase = 0, coutn_uppercase = 0;
    if(s[0] <= 'z' && s[0] >= 'a')
    {
        s[0] = toupper(s[0]);
    }
    cout << s << endl;
}
